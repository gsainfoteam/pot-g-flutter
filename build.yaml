targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
      freezed:
        options:
          when: false

global_options:
  json_serializable:
    runs_before:
      - retrofit_generator
